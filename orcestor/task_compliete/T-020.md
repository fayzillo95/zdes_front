## Task ID: T-020
### Sarlavha: Xatolik holati (error-state) — 3-guruh (positions, terminals, work-schedules, notifications)

**Maqsad (Goal):**
`project_docs/design-reference.md` bo'lim 3.6, 3-qismidagi error-state
patternini quyidagi 4 ta sahifaga qo'llash: `position-list`,
`terminal-list`, `work-schedule-list`, `notification-list`.

**Kontekst (Context):**
Pattern: `design-reference.md` bo'lim 3.6, 3-qism. `notification-list`
BOSHQACHA — jadval emas, `<ul>` ro'yxat (`@if`/`@else if` sintaksisi
ishlatadi, `*ngIf` emas), lekin xuddi shu `loading`/`loadError` signal
g'oyasi qo'llanadi: hozirgi `@else if (notifications().length === 0)`
blokidan oldin `@else if (loadError())` bloki qo'shiladi.

**Cheklovlar (Constraints):**
- FAQAT `.ts`/`.html` fayllarga tegiladi (8 ta fayl). CSS'ga tegilmaydi.
- Filter/actions-cell logikasiga ASLA tegilmaydi (`work-schedule-list`da
  actions-cell bor, TEGILMAYDI).
- `notification-list.ts`da mavjud `error: () => this.loading.set(false)`
  qatoriga `this.loadError.set(true)` va `console.error(...)` qo'shiladi.

**Ish doirasidagi fayllar:**
`src/app/features/positions/pages/position-list/position-list.ts` + `.html`,
`src/app/features/terminals/pages/terminal-list/terminal-list.ts` + `.html`,
`src/app/features/work-schedules/pages/work-schedule-list/work-schedule-list.ts` + `.html`,
`src/app/features/notifications/pages/notification-list/notification-list.ts` + `.html`
(8 ta fayl).

**Bajarilgan deb hisoblanish mezoni (DoD):**
- Har bir sahifada `loadError` to'g'ri ishlaydi.
- `notification-list`da `@else if` zanjiri to'g'ri tartibda
  (loading → loadError → empty → data).
- `npm run build` xatosiz o'tadi.

**Tavsiya etilgan rejim:** Agent-driven.

**Natijani qayerga yozish kerak:**
`orcestor/task_pending/T-020-response.md`ga log, tugagach ikkala
faylni `orcestor/task_compliete/`ga ko'chiring.
