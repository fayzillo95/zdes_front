## Task ID: T-014
### Sarlavha: [Sifat 2/5] Performance holati tahlili (faqat tahlil, kod o'zgarmaydi)

**Ijrochi:** AGY

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-senior-developer.md` uslubida.

**Maqsad (Goal):**
Loyihaning hozirgi performance holatini tahlil qilib
`orcestor/analysis/T-014-performance-analysis.md` ga yozish. **Kod
o'zgartirilmaydi** — faqat tahlil va tavsiyalar.

**Kontekst (Context):**
- Barcha 16 feature moduli lazy-loading (`loadChildren`) orqali ulangan (T-003).
- Ba'zi list komponentlar (masalan `employee-list`, `branch-list`) `ngOnInit`da to'g'ridan-to'g'ri subscribe qiladi, `OnPush` change detection strategiyasi ishlatilmagan.
- `DataTable` komponenti (`shared/components/ui/data-table`) client-side sort/pagination qiladi — katta ma'lumot hajmida (masalan minglab xodim) performance muammosi bo'lishi mumkin.
- `npm run build` natijasida chunk hajmlari ko'rinadi (`dist/` papkasida yoki build log'da).

**Cheklovlar (Constraints):**
- Hech qanday kod fayliga tegilmaydi.
- `npm run build` ni ishga tushirish mumkin (faqat o'qish/tahlil uchun, natijani o'zgartirmaydi).

**Ish doirasidagi fayllar (Files in scope):**
- O'qish uchun: `src/app/**`, `angular.json`, build natijasi (`npm run build` chiqishi)
- Yozish uchun: `orcestor/analysis/T-014-performance-analysis.md` (yangi)

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- Tahlil hujjatida: (1) `npm run build` chunk hajmlari xulosasi (eng katta 5 ta chunk), (2) `OnPush` strategiyasi qo'llanilmagan joylar ro'yxati va tavsiya, (3) `DataTable`ning client-side pagination cheklovlari va backend pagination'ga o'tish tavsiyasi, (4) boshqa aniqlangan performance muammolari (masalan keraksiz `subscribe` tozalanmasligi — memory leak xavfi, `takeUntilDestroyed` tavsiyasi).

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven — faqat tahlil, past risk.

**Natijani qayerga yozish kerak (Report back):**
- `orcestor/task_pending/T-014-response.md`ga qisqa xulosa yozing.
