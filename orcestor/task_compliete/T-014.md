## Task ID: T-014
### Sarlavha: So'rov timeout'i (ildiz tuzatish) + global error-state CSS

**Maqsad (Goal):**
`project_docs/design-reference.md` bo'lim 3.6 (1 va 2-qism) bo'yicha:
(A) `core/services/http.ts`dagi axios instance'ga `timeout` qo'shish —
javob bermayotgan so'rov abadiy osilib qolmasligi uchun (hozirgi holat:
`/holidays` sahifasida kuzatilgan — skeleton loading abadiy aylanaveradi,
sabab shu); (B) `src/styles.css`ga yangi global `.error-state-box`/
`.error-icon`/`.error-text`/`.btn-retry` klasslarini qo'shish (keyingi
tasklar — T-015..T-019 — shu klasslardan foydalanadi).

**Kontekst (Context):**
To'liq CSS/timeout qiymati `design-reference.md` bo'lim 3.6, 1 va
2-qismlarida keltirilgan (nusxa oling, o'zgartirmasdan). Fayllar:
`src/app/core/services/http.ts` (39-41 qatorlar atrofida `axios.create`
chaqiruvi), `src/styles.css`.

**Cheklovlar (Constraints):**
- `http.ts`da FAQAT `axios.create({...})` ichiga `timeout: 15000`
  qo'shiladi — boshqa metod/interceptor logikasi O'ZGARTIRILMAYDI.
- `styles.css`ga FAQAT yangi klasslar QO'SHILADI (mavjud
  `.empty-state-box`/`.loading-state` va boshqa klasslarga tegilmaydi).
- Yangi npm dependency qo'shilmaydi.

**Ish doirasidagi fayllar:** `src/app/core/services/http.ts`,
`src/styles.css` (2 ta fayl).

**Bajarilgan deb hisoblanish mezoni (DoD):**
- `axiosInstance` konfiguratsiyasida `timeout: 15000` mavjud.
- `styles.css`da 4 ta yangi klass qo'shilgan, `--color-danger` tokenidan
  foydalaniladi (hardcoded hex yo'q).
- `npm run build` xatosiz o'tadi.

**Tavsiya etilgan rejim:** Agent-driven (kichik, mexanik, 2 fayl).

**Natijani qayerga yozish kerak:**
`orcestor/task_pending/T-014-response.md`ga log, tugagach ikkala
faylni `orcestor/task_compliete/`ga ko'chiring.
