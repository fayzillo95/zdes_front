## Task ID: T-015
### Sarlavha: [Sifat 3/5] API baseUrl globallashtirish auditi (tahlil + tuzatish)

**Ijrochi:** AGY

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-senior-developer.md` uslubida.

**Maqsad (Goal):**
Barcha `features/*/services/*.ts` fayllarini tekshirib, ularning bari
`core/services/http.ts` dagi yagona `Http` servisidan (va shu orqali
`environment.apiUrl`dan) foydalanishini tasdiqlash, agar birortasi xom
`HttpClient` yoki qattiq kodlangan URL ishlatgan bo'lsa — shuni `Http`
servisiga o'tkazish (bu vazifa **tahlil + tuzatish**, faqat tahlil emas).

**Kontekst (Context):**
- `core/services/http.ts` — barcha so'rovlar `environment.apiUrl` bazasidan foydalanishi kerak bo'lgan yagona nuqta.
- Oldingi modullarda (T-007, Branches) bir marta AGY xato qilib xom `HttpClient` ishlatgan edi — Claude Code qo'lda tuzatgan. Bu holat boshqa joyda ham takrorlangan bo'lishi mumkin, tekshirilmagan.
- Har bir servis fayli: `advance.ts`, `attendance.ts`, `branch.ts`, `company.ts`, `department.ts`, `employee.ts`, `holiday.ts`, `leave.ts`, `notification.ts`, `payroll.ts`, `position.ts`, `salary-adjustment.ts`, `setting.ts`, `terminal.ts`, `work-schedule.ts` (`src/app/features/*/services/`).

**Cheklovlar (Constraints):**
- Faqat `features/*/services/*.ts` fayllariga tegining, komponentlarga tegmang (agar komponent to'g'ridan-to'g'ri `HttpClient` ishlatayotgani aniqlansa, alohida eslatib qo'ying, lekin bu vazifa doirasida tuzatmang — keyingi taskga qoldiring).
- Yangi npm dependency qo'shmang.

**Ish doirasidagi fayllar (Files in scope):**
- `src/app/features/*/services/*.ts` (o'qish va zarur bo'lsa tuzatish)
- Yozish uchun: `orcestor/analysis/T-015-api-audit.md` (yangi, topilmalar ro'yxati)

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- `orcestor/analysis/T-015-api-audit.md`da har bir servis fayli uchun holat jadvali (`Http` ishlatadi / xom `HttpClient` ishlatadi / boshqa muammo).
- Aniqlangan barcha xom `HttpClient` holatlar `Http` servisiga o'tkazilgan.
- `npm run build` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven.

**Natijani qayerga yozish kerak (Report back):**
- `orcestor/task_pending/T-015-response.md`ga tekshirilgan fayllar va tuzatilganlar ro'yxatini yozing.
