## Task ID: T-007
### Sarlavha: [Modul 5/10] Tashkiliy tuzilma — Company, Branches, Departments, Positions (CRUD)

**Ijrochi:** AGY

**Bog'liqlik:** Modul 2 (Auth, T-004) va Modul 3 (Shared UI Kit, T-005)
tugagandan KEYIN boshlanishi kerak — bu modul `DataTable` va `ConfirmDialog`
komponentlaridan foydalanadi.

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-frontend-developer.md` uslubida —
bu vazifa asosan takrorlanuvchi (repetitive) CRUD ekranlar qurish, shuning
uchun izchillik (consistency) va DRY muhim: 4 ta feature bir xil naqshga
(pattern) amal qilishi kerak.

**Maqsad (Goal):**
`Company`, `Branches`, `Departments`, `Positions` feature'larini to'liq CRUD
ekranlar (list/form/detail) va ularning servis+model qatlami bilan ishlaydigan
holga keltirish.

**Kontekst (Context):**
- Modellar (`core/models/company.ts`, `branch.ts`, `department.ts`, `position.ts`)
  hozircha **bo'sh fayllar** — shu vazifa doirasida to'ldiriladi (interfeys
  sifatida: `id`, `name`, va har bir entity uchun mantiqiy qo'shimcha maydonlar,
  masalan `department.branchId`, `position.departmentId` — tashkiliy iyerarxiya
  o'zaro bog'liq bo'lishi kutiladi).
- Har bir feature'da `*-module.ts` (NgModule shell, `declarations: []`) va
  `*-routing-module.ts` (`RouterModule.forChild(routes)`, `routes` bo'sh) bor —
  T-003'da qurilgan konventsiyaga ko'ra, bu NgModule'lar o'zgarmaydi, faqat
  `routes` massivi to'ldiriladi va standalone page komponentlariga ishora qiladi
  (masalan `{ path: '', component: BranchList }`).
- `Company` boshqalardan farqli — faqat `company-detail` sahifasi bor, list
  yo'q (bitta kompaniya konfiguratsiyasi, singleton hisoblanadi).
- `Branches`, `Departments`, `Positions` — har birida `*-list` va `*-form`
  sahifalari bor (detail sahifasi yo'q — list+form yetarli).
- Har bir feature'ning `services/*.ts` fayli bo'sh stub (`Injectable`, bo'sh
  klass) — `core/services/http.ts` (T-003) dan foydalanib CRUD metodlar bilan
  to'ldiriladi.
- `shared/components/ui/data-table`, `confirm-dialog` (T-005) list sahifalarida ishlatiladi.

**Cheklovlar (Constraints):**
- Faqat quyidagi fayllarga tegining — boshqa feature'larga (`employees`,
  `attendance` va h.k.) tegmang.
- Yangi npm dependency qo'shmang.
- 4 ta feature bir xil CRUD naqshiga amal qilsin (servis metodlari nomi:
  `getAll()`, `getById(id)`, `create(dto)`, `update(id, dto)`, `delete(id)`).
- Backend endpoint yo'llari taxminiy: `/companies`, `/branches`, `/departments`,
  `/positions` (izohda belgilang).

**Ish doirasidagi fayllar (Files in scope):**
- `src/app/core/models/company.ts`, `branch.ts`, `department.ts`, `position.ts`
- `src/app/features/company/**` (module, routing-module, service, `company-detail` sahifasi)
- `src/app/features/branches/**`
- `src/app/features/departments/**`
- `src/app/features/positions/**`

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- 4 ta model to'liq tiplashtirilgan interfeys sifatida yozilgan.
- 4 ta servis `Http` orqali CRUD metodlar bilan ishlaydi.
- `Branches`/`Departments`/`Positions` — list sahifasi `DataTable`da ma'lumotlarni ko'rsatadi, o'chirish `ConfirmDialog` orqali tasdiqlanadi, form sahifasi yaratish/tahrirlash uchun ishlaydi (reactive form).
- `Company` — detail/tahrirlash sahifasi bitta yozuv bilan ishlaydi.
- Har bir feature'ning `*-routing-module.ts` to'ldirilgan (list/form/detail route'lari).
- `npm run build` va `npx tsc --noEmit` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven.

**Natijani qayerga yozish kerak (Report back):**
- Har bir muhim qadamni darhol `orcestor/task_pending/T-007-response.md`ga yozib boring.
- Tugagach: yakuniy xulosa qo'shib, `T-007.md` va `T-007-response.md`ni `orcestor/task_compliete/`ga ko'chiring.
