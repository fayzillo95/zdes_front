## Task ID: T-006
### Sarlavha: [Modul 4/10] Dashboard — boshqaruv paneli (placeholder/summary)

**Ijrochi:** Claude Code (o'zim bajaraman — AGY'ga dispatch qilinmaydi)

**Sabab (nima uchun AGY'ga emas):** Hajman kichik, arxitektura riski past
(faqat bitta sahifa, boshqa modullarga bog'liqlik yo'q). AGY resursi cheklangan
bo'lgani uchun bunday yengil vazifalar Claude Code tomonidan to'g'ridan-to'g'ri
bajariladi — task fayli baribir rejalashtirish va DoD nazorati izchilligi
uchun yoziladi.

**Maqsad (Goal):**
`Dashboard` sahifasini asosiy statistik ko'rsatkichlar uchun placeholder-darajadagi
UI bilan to'ldirish (jami xodimlar soni, bugungi davomat foizi, oxirgi
bildirishnomalar ro'yxati kabi kartochkalar).

**Kontekst (Context):**
- `src/app/features/dashboard/pages/dashboard/dashboard.ts` — bo'sh standalone komponent.
- `src/app/features/dashboard/dashboard-routing-module.ts` — `routes` massivi bo'sh, `Dashboard` komponentiga yo'naltirilmagan.
- Backend hali ulanmagan — statistik ma'lumotlar uchun hozircha placeholder/mock qiymatlar ishlatiladi (masalan `employeeCount = 0` yoki staticheskiy namuna raqam), keyinchalik `Employee`/`Attendance` servislariga ulanadi.

**Cheklovlar (Constraints):**
- Boshqa feature modullarga tegilmaydi.
- Yangi npm dependency qo'shilmaydi (chart kutubxonasi kerak bo'lmaydi — oddiy kartochka/raqam ko'rinishida yetarli).
- `shared/` componentlariga (Modul 3, hali tugamagan bo'lishi mumkin) qattiq bog'lanib qolinmaydi — agar `DataTable` va h.k. hali tayyor bo'lmasa, oddiy HTML/CSS kartochkalar bilan cheklaniladi.

**Ish doirasidagi fayllar (Files in scope):**
- `src/app/features/dashboard/pages/dashboard/dashboard.ts`, `dashboard.html`, `dashboard.css`
- `src/app/features/dashboard/dashboard-routing-module.ts`

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- `dashboard-routing-module.ts`da `''` path `Dashboard` komponentiga yo'naltirilgan.
- Sahifada kamida 3 ta statistik kartochka (placeholder qiymatlar bilan) va oddiy layout mavjud.
- `ng build` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
N/A — AGY'ga dispatch qilinmaydi, Claude Code tomonidan to'g'ridan-to'g'ri bajariladi.

**Natijani qayerga yozish kerak (Report back):**
Bajarilgach, ushbu faylni to'g'ridan-to'g'ri `orcestor/task_compliete/T-006.md`ga
ko'chirib, oxiriga bajarilgan ishlar xulosasini qo'shing (AGY response log
kerak emas, chunki AGY ishtirok etmadi).

---

## Yakuniy xulosa (Claude Code, 2026-07-25)

Bajarildi — to'g'ridan-to'g'ri Claude Code tomonidan (AGY ishtirok etmadi):
- `dashboard.ts` — 4 ta placeholder statistik ko'rsatkich (`stats` massivi: jami xodimlar, bugungi davomat, ochiq ta'til so'rovlari, o'qilmagan bildirishnomalar).
- `dashboard.html` — `@for` bilan statistik kartochkalarni render qiladi.
- `dashboard.css` — responsive grid layout (`auto-fit`), kartochka stillari.
- `dashboard-routing-module.ts` — `{ path: '', component: Dashboard }` qo'shildi.
- `npx tsc --noEmit` va `npm run build` — ikkalasi ham xatosiz o'tdi.

Definition of Done to'liq bajarildi.
