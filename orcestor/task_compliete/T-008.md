## Task ID: T-008
### Sarlavha: [Modul 6/10] Xodimlar (Employees) — CRUD + Face Register

**Ijrochi:** AGY

**Bog'liqlik:** Modul 2 (Auth), Modul 3 (Shared UI Kit) va Modul 5 (Tashkiliy
tuzilma — T-007, chunki xodim forma sahifasida bo'lim/lavozim/filial
tanlash kerak) tugagandan KEYIN boshlanishi kerak.

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-frontend-developer.md` uslubida.

**Maqsad (Goal):**
`Employees` feature'ini to'liq CRUD (list/form/detail) va `face-register`
komponentini (kamera orqali xodim yuzini ro'yxatga olish UI) ishlaydigan
holga keltirish.

**Kontekst (Context):**
- `src/app/core/models/employee.ts` — hozircha **butunlay bo'sh fayl**,
  interfeys shu vazifada yoziladi (`id`, `fullName` yoki `firstName`/`lastName`,
  `branchId`, `departmentId`, `positionId`, `status`, `phone`, `hiredAt` kabi
  maydonlar bilan — `core/models/branch.ts`/`department.ts`/`position.ts`
  (T-007) bilan bog'liq bo'lsin).
- `src/app/features/employees/services/employee.ts` — bo'sh stub, `Http`
  orqali CRUD bilan to'ldiriladi.
- `src/app/features/employees/pages/employee-list`, `employee-form`,
  `employee-detail` — bo'sh standalone komponentlar.
- `src/app/features/employees/components/face-register/face-register.ts` —
  bo'sh standalone komponent; `shared/components/ui/camera-capture` (T-005)
  dan foydalanib xodim rasmini olib, `Employee` servisi orqali (placeholder
  endpoint, masalan `POST /employees/:id/face`) yuborishi kerak.
- `employee-form` sahifasida bo'lim/lavozim/filial tanlash uchun
  `Department`/`Position`/`Branch` servislaridan (T-007) foydalaniladi.

**Cheklovlar (Constraints):**
- Faqat `employees` feature papkasi va `core/models/employee.ts`ga tegining.
- Yangi npm dependency qo'shmang.
- `face-register` — `CameraCapture` componentini **qayta ishlatadi** (uni
  qayta yozmaydi), faqat uni employee kontekstida ishlatadi.
- Backend endpoint yo'llari taxminiy: `/employees`, `/employees/:id/face` (izohda belgilang).

**Ish doirasidagi fayllar (Files in scope):**
- `src/app/core/models/employee.ts`
- `src/app/features/employees/services/employee.ts`
- `src/app/features/employees/pages/employee-list/*`
- `src/app/features/employees/pages/employee-form/*`
- `src/app/features/employees/pages/employee-detail/*`
- `src/app/features/employees/components/face-register/*`
- `src/app/features/employees/employees-routing-module.ts`

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- `Employee` modeli to'liq tiplashtirilgan.
- `employee-list` — `DataTable`da xodimlar ro'yxatini (ism, bo'lim, lavozim, filial, status ustunlari bilan) ko'rsatadi, o'chirish `ConfirmDialog` orqali.
- `employee-form` — reactive form, bo'lim/lavozim/filial uchun dropdown (tegishli servislardan ma'lumot olib), yaratish/tahrirlash ishlaydi.
- `employee-detail` — bitta xodim ma'lumotini to'liq ko'rsatadi.
- `face-register` — `CameraCapture` orqali rasmga oladi va yuborish tugmasi bilan ishlaydi (backend hali yo'q bo'lsa ham, so'rov chaqiruvi to'g'ri yozilgan bo'lsin).
- Routing to'liq (list/form/:id/edit/:id/detail va h.k.).
- `npm run build` va `npx tsc --noEmit` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven.

**Natijani qayerga yozish kerak (Report back):**
- Har bir muhim qadamni darhol `orcestor/task_pending/T-008-response.md`ga yozib boring.
- Tugagach: yakuniy xulosa qo'shib, `T-008.md` va `T-008-response.md`ni `orcestor/task_compliete/`ga ko'chiring.
