## Task ID: T-011b
### Sarlavha: Column-level jadval filter — 3-guruh (work-schedules, attendance, payroll)

**Maqsad (Goal):**
Quyidagi 3 ta list sahifasiga `project_docs/design-reference.md` bo'lim
3.2 ("Column-level jadval filter patterni") ni qo'llash.

**Kontekst (Context):**
Ishlaydigan TO'LIQ namuna: `src/app/features/branches/pages/branch-list/
branch-list.ts` va `.html`. To'liq qoidalar: `project_docs/
design-reference.md` bo'lim 3.2.

**MUHIM CHEKLOV:** `work-schedule-list`ning "Amallar" ustuni ALLAQACHON
to'g'ri (faqat delete, SVG) — TEGMANG. `attendance-list`da Amallar
ustuni UMUMAN YO'Q va QO'SHILMAYDI. `payroll-list`da ham Amallar
ustuni YO'Q va QO'SHILMAYDI (avval bor edi, ataylab olib tashlangan,
qayta tiklanmaydi). Uchalasida ham FAQAT filter qatori qo'shiladi.

**Har bir sahifa uchun ustun-filter jadvali:**

### 1. `src/app/features/work-schedules/pages/work-schedule-list/work-schedule-list.ts` + `.html`
| Ustun | Filter turi | Manba |
|---|---|---|
| # | yo'q | — |
| Grafik Nomi | matn | `schedule.name` |
| Kompaniya | matn | `getCompanyName(schedule.companyId)` natijasi bo'yicha |
| Filial | matn | `getBranchName(schedule.branchId)` natijasi bo'yicha |
| Ish Vaqti | yo'q | — |
| Ish Kunlari | yo'q | — |
| Kechikish | yo'q | — |
| Birlamchi | select | `schedule.isDefault` (boolean): option'lar "Hammasi"(bo'sh)/"Ha"(true)/"Yo'q"(false) |
| Amallar | yo'q (TEGMANG) | — |
Signal nomlari: `nameFilter`, `companyFilter`, `branchFilter`,
`isDefaultFilter` (`'' | 'yes' | 'no'`). Metod nomi:
`filteredWorkSchedules` (`workSchedules` plain array — oddiy metod).

### 2. `src/app/features/attendance/pages/attendance-list/attendance-list.ts` + `.html`
| Ustun | Filter turi | Manba |
|---|---|---|
| # | yo'q | — |
| Xodim | matn | `getEmployeeName(record.employeeId)` natijasi bo'yicha filtr |
| Sana | sana | `record.date` |
| Kirish vaqti | yo'q | — |
| Chiqish vaqti | yo'q | — |
| Ishlangan (Daq) | yo'q | — |
| Kechikish (Daq) | yo'q | — |
| Holat | select | `record.status`: option'lar `present`(Kelgan)/`absent`(Kelmagan)/`late`(Kechikkan) + "Hammasi" |
Signal nomlari: `employeeFilter`, `dateFilter`, `statusFilter`. Metod
nomi: `filteredAttendances` (`attendances` plain array — oddiy metod).
Bu sahifada Amallar ustuni yo'q — filter qatoridagi ustunlar soni
header ustunlari soniga (8 ta) mos kelishi kerak, oxirgi (Holat)
ustunidan keyin qo'shimcha bo'sh `<th>` QO'SHILMAYDI.

### 3. `src/app/features/payroll/pages/payroll-list/payroll-list.ts` + `.html`
| Ustun | Filter turi | Manba |
|---|---|---|
| Xodim ID | matn | `payroll.employeeId` |
| Davr | matn | `payroll.month` |
| Asosiy summa | yo'q | — |
| Ushlab qolingan | yo'q | — |
| Yakuniy summa | yo'q | — |
Signal nomlari: `employeeIdFilter`, `monthFilter`. `payrolls` bu faylda
signal (`payrolls = signal<Payroll[]>([])`) — `computed()` orqali
`filteredPayrolls` yozing (bu fayl signal-asosli, boshqalaridan farqli,
`branch-list` namunasidagi kabi `computed()` ishlatiladi).

**Cheklovlar (Constraints):**
- FAQAT yuqoridagi 3 ta modulning `.ts` va `.html` fayllariga tegiladi
  (jami 6 fayl). CSS fayllarga tegilmaydi.
- Amallar ustuni/actions-cell hech qanday shaklda QO'SHILMAYDI yoki
  o'zgartirilmaydi (`work-schedule-list`da mavjudi tegilmaydi,
  `attendance-list`/`payroll-list`da yo'qligi saqlanadi).
- `FormsModule` har bir `.ts`ga import qilinadi (agar yo'q bo'lsa).
- Yangi npm dependency qo'shilmaydi.

**Ish doirasidagi fayllar:** 6 ta fayl (3 modul × ts/html).

**Bajarilgan deb hisoblanish mezoni (DoD):**
- Filter qatori thead ichida, ishlaydi, ustunlar soni header bilan mos.
- "Filterga mos topilmadi" holati mavjud.
- `npm run build` xatosiz o'tadi.

**Tavsiya etilgan rejim:** Agent-driven.

**Natijani qayerga yozish kerak:**
`orcestor/task_pending/T-011b-response.md`ga log, tugagach ikkala
faylni `orcestor/task_compliete/`ga ko'chiring.
