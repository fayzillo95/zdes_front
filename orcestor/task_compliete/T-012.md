## Task ID: T-012
### Sarlavha: [Modul 10/10] Admin — Settings, Notifications

**Ijrochi:** Claude Code (o'zim bajaraman — AGY'ga dispatch qilinmaydi)

**Sabab (nima uchun AGY'ga emas):** Hajman kichik (2 ta feature, murakkab
o'zaro bog'liqlik yo'q), arxitektura riski past. AGY resursi cheklangan
bo'lgani uchun bunday yengil vazifalar Claude Code tomonidan to'g'ridan-to'g'ri
bajariladi.

**Maqsad (Goal):**
`Settings` (kompaniya darajasidagi konfiguratsiya formasi) va `Notifications`
(bildirishnomalar ro'yxati, o'qilgan deb belgilash) feature'larini ishlaydigan
holga keltirish.

**Kontekst (Context):**
- `src/app/features/settings/pages/settings-page/settings-page.ts` va
  `src/app/features/settings/services/setting.ts` — bo'sh stub. Alohida
  model fayli yo'q — `Setting` uchun oddiy key-value yoki forma-asosidagi
  interfeys shu servis faylida yoki yangi `core/models/setting.ts`da
  belgilanishi mumkin.
- `src/app/features/notifications/pages/notification-list/notification-list.ts`,
  `services/notification.ts` — bo'sh stub. `core/models/notification.ts`
  mavjud, lekin bo'sh — shu vazifada to'ldiriladi (`id`, `title`, `message`,
  `read: boolean`, `createdAt`).
- `Header`dagi (T-005) bildirishnoma belgisi (agar mavjud bo'lsa) bu
  ro'yxatga bog'lanishi mumkin, lekin bu ixtiyoriy — asosiy talab
  `notification-list` sahifasining o'zi ishlashi.

**Cheklovlar (Constraints):**
- Faqat `settings`, `notifications` feature papkalari va `core/models/notification.ts`ga tegining.
- Yangi npm dependency qo'shmang.
- `DataTable`/`ConfirmDialog` (agar T-005 tugagan bo'lsa) ishlatilishi mumkin, lekin majburiy emas — oddiy ro'yxat ham yetarli.
- Backend endpoint yo'llari taxminiy: `/settings`, `/notifications` (izohda belgilang).

**Ish doirasidagi fayllar (Files in scope):**
- `src/app/core/models/notification.ts`
- `src/app/features/settings/**`
- `src/app/features/notifications/**`

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- `Notification` modeli to'liq tiplashtirilgan.
- `settings-page` — kamida bir nechta forma maydoni bilan (masalan kompaniya nomi, valyuta, ish vaqti) saqlash tugmasi ishlaydi.
- `notification-list` — bildirishnomalar ro'yxatini ko'rsatadi, "o'qilgan deb belgilash" harakati ishlaydi.
- Routing to'liq.
- `ng build` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
N/A — AGY'ga dispatch qilinmaydi, Claude Code tomonidan to'g'ridan-to'g'ri bajariladi.

**Natijani qayerga yozish kerak (Report back):**
Bajarilgach, ushbu faylni to'g'ridan-to'g'ri `orcestor/task_compliete/T-012.md`ga
ko'chirib, oxiriga bajarilgan ishlar xulosasini qo'shing.

---

## Yakuniy xulosa (Claude Code, 2026-07-25)

Bajarildi — to'g'ridan-to'g'ri Claude Code tomonidan (AGY ishtirok etmadi,
kvota tugagan davrda bajarildi):

- `core/models/notification.ts` — `Notification` interfeysi (`id`, `title`, `message`, `read`, `createdAt`).
- `features/notifications/services/notification.ts` — `NotificationService` klassi (nomi `Notification` model bilan to'qnashmasligi uchun shunday nomlandi), `getAll()`/`markAsRead(id)`, `Http` orqali `/notifications`.
- `features/notifications/pages/notification-list/*` — ro'yxat, "o'qildi deb belgilash" tugmasi, o'qilmagan yozuvlar vizual belgilangan.
- `notifications-routing-module.ts` — `{ path: '', component: NotificationList }`.
- `features/settings/services/setting.ts` — `CompanySettings` interfeysi shu faylda, `Setting` servisi `get()`/`update()`.
- `features/settings/pages/settings-page/*` — reactive form (`nonNullable.group`): kompaniya nomi, valyuta, ish kuni boshlanishi/tugashi, saqlash tugmasi.
- `settings-routing-module.ts` — `{ path: '', component: SettingsPage }`.
- `npx tsc --noEmit` va `npm run build` — ikkalasi ham xatosiz o'tdi (bitta oldindan mavjud, T-004'ga tegishli `login.css` budget ogohlantirishidan tashqari).

Definition of Done to'liq bajarildi.
