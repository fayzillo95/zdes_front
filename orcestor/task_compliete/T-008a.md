## Task ID: T-008a
### Sarlavha: Column-level jadval filter — 1-guruh (company, employees, departments)

**Maqsad (Goal):**
Quyidagi 3 ta list sahifasiga `project_docs/design-reference.md` bo'lim
3.2'da tasvirlangan column-level filter patternini qo'llash — har bir
sahifa uchun aniq ustun-tur jadvali quyida berilgan.

**Kontekst (Context):**
Ishlaydigan TO'LIQ namuna: `src/app/features/branches/pages/branch-list/
branch-list.ts` va `branch-list.html` — aynan shu patternni (signal
e'lonlari, `computed()` filter funksiyasi, `column-filter-row` HTML
qatori, bo'sh-natija holati) har bir quyidagi sahifaga moslashtiring.
To'liq qoidalar: `project_docs/design-reference.md` bo'lim 3.2.

**Har bir sahifa uchun ustun-filter jadvali:**

### 1. `src/app/features/company/pages/company-list/company-list.ts` + `.html`
| Ustun | Filter turi | Maydon |
|---|---|---|
| # | yo'q | — |
| Kompaniya Nomi | matn | `name` |
| Yuridik Nomi | matn | `legalName` |
| Telefon | matn | `phone` |
| Email | matn | `email` |
| Holat | select (Hammasi/Faol/Nofaol) | `isActive` |
| Amallar | yo'q | — |
Signal nomlari: `nameFilter`, `legalNameFilter`, `phoneFilter`,
`emailFilter`, `statusFilter`. Computed: `filteredCompanies`.
`@for`da `companies()` o'rniga `filteredCompanies()` ishlating.

### 2. `src/app/features/employees/pages/employee-list/employee-list.ts` + `.html`
| Ustun | Filter turi | Maydon |
|---|---|---|
| # | yo'q | — |
| Xodim Nomi | matn | `firstName`/`lastName`/`fullName` (bittasi mavjud bo'lsa) |
| Telefon | matn | `phone` |
| Email | matn | `email` |
| Holat | select (Hammasi/Faol/Nofaol) | `isActive` |
| Amallar | yo'q | — |
Signal nomlari: `nameFilter`, `phoneFilter`, `emailFilter`,
`statusFilter`. Computed: `filteredEmployees`. `@for`da `employees()`
o'rniga `filteredEmployees()` ishlating. "Xodim Nomi" filter matni
`firstName`, `lastName`, `fullName` maydonlaridan istalgani mos kelsa
yetarli (OR mantiq).

### 3. `src/app/features/departments/pages/department-list/department-list.ts` + `.html`
| Ustun | Filter turi | Maydon |
|---|---|---|
| Bo'lim Nomi | matn | `name` |
| ID | yo'q | — |
| Amallar | yo'q | — |
Signal nomi: `nameFilter`. Computed: `filteredDepartments`. `@for`da
`departments()` o'rniga `filteredDepartments()` ishlating.

**Cheklovlar (Constraints):**
- FAQAT yuqoridagi 3 ta modulning `.ts` va `.html` fayllariga tegiladi
  (jami 6 fayl). Boshqa hech qanday faylga tegilmaydi.
- `FormsModule` har bir `.ts` faylning import'iga va `@Component`
  `imports` massiviga qo'shiladi (agar hali yo'q bo'lsa).
- Signal-asosli sahifalarda (`signal()`/`computed()` mavjud bo'lsa)
  xuddi shu uslubda davom eting. Agar sahifa signal ishlatmasa (plain
  property), `computed()` o'rniga oddiy getter metod yozing va
  `@for`da metod chaqiruvi (`filteredX()`) ishlating.
- Mavjud `loading`/`skeleton` va boshqa funksionallikka (delete,
  onRowClick va h.k.) tegilmaydi — faqat filter qo'shiladi.
- Yangi npm dependency qo'shilmaydi.

**Ish doirasidagi fayllar (Files in scope):**
6 ta fayl: yuqorida ro'yxatlangan 3 ta modulning `.ts` + `.html`.

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- Har bir sahifada filter qatori thead ichida, jadval header'idan
  keyin ko'rinadi.
- Filter ishlaydi (matn — substring, select — aniq moslik).
- "Filterga mos topilmadi" holati mavjud (asosiy bo'sh-holatdan alohida).
- `npm run build` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven — aniq namuna va jadval asosida.

**Natijani qayerga yozish kerak (Report back):**
- `orcestor/task_pending/T-008a-response.md` ga qadamlarni yozing.
- Tugagach yakuniy xulosa + "O'zgargan fayllar" jadvalini qo'shing,
  `T-008a.md` va `T-008a-response.md` ni `orcestor/task_compliete/`ga
  ko'chiring.
