## Task ID: T-010
### Sarlavha: [Modul 8/10] Ta'til va G'oyiblik — Leaves, Holidays, Advances

**Ijrochi:** AGY

**Bog'liqlik:** Modul 2 (Auth), Modul 3 (Shared UI Kit), Modul 6 (Employees,
T-008 — chunki ta'til/avans yozuvlari xodimga bog'lanadi) tugagandan KEYIN.

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-frontend-developer.md` uslubida.

**Maqsad (Goal):**
`Leaves`, `Holidays`, `Advances` feature'larini to'liq CRUD (list/form)
ekranlari bilan ishlaydigan holga keltirish.

**Kontekst (Context):**
- Modellar: `core/models/employee-leave.ts`, `holiday.ts`, `advance.ts` —
  hozircha bo'sh, shu vazifada to'ldiriladi.
- `Leaves` (`employee-leave` modeli) — xodimga (`employeeId`), sana
  oralig'iga (`startDate`, `endDate`) va turga (`type`: masalan
  ta'til/kasallik/bepul) bog'liq.
- `Holidays` — kompaniya bo'yicha umumiy dam olish kunlari (sana + nom),
  xodimga bog'liq emas.
- `Advances` — xodimga (`employeeId`) va summaga (`amount`) bog'liq avans
  so'rovlari.
- Har uch feature'da ham faqat `*-list` va `*-form` sahifalari bor (detail
  sahifasi yo'q).

**Cheklovlar (Constraints):**
- Faqat `leaves`, `holidays`, `advances` feature papkalari va tegishli 3 ta
  model fayliga tegining.
- Yangi npm dependency qo'shmang (sana tanlash uchun native
  `<input type="date">`dan foydalaning).
- `DataTable`/`ConfirmDialog`dan (T-005) foydalaning, T-007'dagi CRUD
  naqshiga rioya qiling.
- Backend endpoint yo'llari taxminiy: `/leaves`, `/holidays`, `/advances` (izohda belgilang).

**Ish doirasidagi fayllar (Files in scope):**
- `src/app/core/models/employee-leave.ts`, `holiday.ts`, `advance.ts`
- `src/app/features/leaves/**`
- `src/app/features/holidays/**`
- `src/app/features/advances/**`

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- 3 ta model to'liq tiplashtirilgan.
- Har uch feature — `DataTable`da ro'yxat, `ConfirmDialog` bilan o'chirish, reactive form bilan yaratish/tahrirlash ishlaydi.
- `Leaves`/`Advances` formalarida xodim tanlash (`Employee` servisidan) mavjud.
- Routing to'liq.
- `npm run build` va `npx tsc --noEmit` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven.

**Natijani qayerga yozish kerak (Report back):**
- Har bir muhim qadamni darhol `orcestor/task_pending/T-010-response.md`ga yozib boring.
- Tugagach: yakuniy xulosa qo'shib, `T-010.md` va `T-010-response.md`ni `orcestor/task_compliete/`ga ko'chiring.
