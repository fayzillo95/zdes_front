## Task ID: T-010a
### Sarlavha: Actions-cell standartlashtirish + column filter — 1-guruh (advances, holidays, salary-adjustments)

**Maqsad (Goal):**
Quyidagi 3 ta list sahifasiga IKKALA narsani birga qo'llash:
(A) `project_docs/design-reference.md` bo'lim 3.3 ("Kanonik Amallar
ustuni patterni") — eski matnli `edit-btn`/`delete-btn` o'rniga SVG
icon `.btn-edit`/`.btn-delete` pattern;
(B) bo'lim 3.2 ("Column-level jadval filter patterni") — matn/sana
ustunlariga filter.

**Kontekst (Context):**
Ikkala TO'LIQ ishlaydigan namuna:
- Actions-cell: `src/app/features/branches/pages/branch-list/branch-list.html`
  (SVG edit+delete, `design-reference.md` bo'lim 3.3'da aynan shu
  HTML/CSS ko'chirilgan).
- Filter: `src/app/features/branches/pages/branch-list/branch-list.ts`
  (`computed()` filter) va bo'lim 3.2.

**Har bir sahifa uchun aniq ko'rsatma:**

### 1. `src/app/features/advances/pages/advance-list/advance-list.ts` + `.html` + `.css`
- Ustun-filter jadvali: ID(yo'q), Xodim ID(matn, `employeeId`),
  Summa(yo'q), Sabab(matn, `reason`), Sana(sana, `date`), Amallar(yo'q).
  Signal nomlari: `employeeIdFilter`, `reasonFilter`, `dateFilter`.
  Computed nomi: `filteredAdvances`. `@for`da `advances` (plain array,
  signal EMAS bu faylda) o'rniga `filteredAdvances()` metod/getter
  ishlating (`computed()` signal sifatida qo'shing, `advances` massivi
  o'zi ham signalga aylantirilishi SHART EMAS — `filteredAdvances`
  computed ichida `this.advances` ga to'g'ridan-to'g'ri murojaat qiling,
  lekin bu holda oddiy getter metod yozing, chunki `computed()` faqat
  signal reaktivligini kuzatadi, plain propertyni emas — shu sabab bu
  faylda ODDIY METOD yozing: `filteredAdvances(): Advance[] { ... }`
  (computed emas), va HTML template'da metod chaqiruvi sifatida ishlating.
- Actions-cell: mavjud edit link `['edit', advance.id]` route'ini
  SAQLANG, faqat vizual pattern (matn o'rniga SVG icon, `edit-btn`/
  `delete-btn` klasslari o'rniga `btn-edit`/`btn-delete`) almashtiring.
  `.css`dagi `.edit-btn`/`.delete-btn`/`.action-buttons` qoidalarini
  `design-reference.md` bo'lim 3.3'dagi kanonik `.actions-cell`/
  `.btn-action`/`.btn-edit`/`.btn-delete` CSS bilan almashtiring.

### 2. `src/app/features/holidays/pages/holiday-list/holiday-list.ts` + `.html` + `.css`
- Ustun-filter: ID(yo'q), Nomi(matn, `name`), Sana(sana, `startDate`),
  Amallar(yo'q). Signal: `nameFilter`, `startDateFilter`. Computed/
  metod nomi: `filteredHolidays` (bu faylda ham `holidays` plain array —
  yuqoridagi advances'dagi kabi oddiy metod yozing, computed emas).
- Actions-cell: mavjud edit route `[holiday.id, 'edit']` SAQLANSIN,
  faqat vizual pattern SVG'ga almashtiriladi (yuqoridagi kabi).

### 3. `src/app/features/salary-adjustments/pages/adjustment-list/adjustment-list.ts` + `.html` + `.css`
- Ustun-filter: ID(yo'q), Xodim ID(matn, `employeeId`), Summa(yo'q),
  Turi(matn, `type`), Sabab(matn, `reason`), Sana(sana, `date`),
  Amallar(yo'q). Signal: `employeeIdFilter`, `typeFilter`,
  `reasonFilter`, `dateFilter`. Metod nomi: `filteredAdjustments`
  (bu faylda ham plain array — oddiy metod).
- Actions-cell: mavjud edit route `[item.id, 'edit']` SAQLANSIN, vizual
  pattern SVG'ga almashtiriladi.

**Cheklovlar (Constraints):**
- FAQAT yuqoridagi 3 ta modulning `.ts`, `.html`, `.css` fayllariga
  tegiladi (jami 9 fayl).
- Mavjud edit/delete route'lar (`routerLink` qiymatlari) O'ZGARTIRILMAYDI
  — faqat vizual ko'rinish (SVG ikonka) almashadi.
- `FormsModule` har bir `.ts`ga import qilinadi (agar yo'q bo'lsa).
- Delete metod chaqiruvlari (masalan `deleteAdvance(id)`) o'zgarishsiz
  qoladi.
- Yangi npm dependency qo'shilmaydi.

**Ish doirasidagi fayllar:** 9 ta fayl (3 modul × ts/html/css).

**Bajarilgan deb hisoblanish mezoni (DoD):**
- Har bir sahifada filter qatori va SVG edit+delete tugmalari ishlaydi.
- Eski `edit-btn`/`delete-btn`/emoji hech qayerda qolmagan.
- `npm run build` xatosiz o'tadi.

**Tavsiya etilgan rejim:** Agent-driven.

**Natijani qayerga yozish kerak:**
`orcestor/task_pending/T-010a-response.md`ga log, tugagach ikkala
faylni `orcestor/task_compliete/`ga ko'chiring.
