## Task ID: T-018
### Sarlavha: [Sifat 6/6] Dublikat/tashlab ketilgan (orphaned) fayllar auditi va tozalash

**Ijrochi:** AGY

**Sabab (kontekst):** Oldingi dispatch'larda AGY ba'zan berilgan stub
fayllarni tahrirlash o'rniga YANGI nomli fayl yaratgan (masalan
`branch.service.ts` asl `branch.ts` o'rniga, yoki `confirm-dialog.component.ts`
asl `confirm-dialog.ts` o'rniga) — bu holatlar ko'pincha qo'lda topilib
tuzatilgan, lekin ba'zilari (masalan T-005'dagi birinchi muvaffaqiyatsiz
urinishdan qolgan `confirm-dialog.component.*` va `image-upload.component.*`,
jami 6 ta fayl) hech qanday build xatosiga sabab bo'lmagani uchun
sezilmay qolgan edi (chunki hech yerda import qilinmagan — "o'lik kod").
Fayzillo bu turdagi dublikatlarni butun loyiha bo'yicha qidirib chiqishni
so'radi.

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-senior-developer.md` uslubida —
kodni kengroq ko'lamda tekshirish va tozalash.

**Maqsad (Goal):**
Butun `src/app/` bo'yicha dublikat/ishlatilmayotgan (orphaned) fayllarni
topib, ularni o'chirish yoki (agar noaniq bo'lsa) ro'yxatga olib xabar
berish.

**Kontekst (Context):**
- Loyihada har bir komponent/servis uchun **bitta** fayl bo'lishi kerak
  (masalan `branch.ts`, `branch-list.ts` — .component/.service qo'shimcha
  suffikslarisiz).
- Shubhali naqshlar: `*.component.ts`, `*.component.html`, `*.component.css`,
  `*.service.ts` (agar yonida suffikssiz asl fayl ham bo'lsa), `*-routing.module.ts`
  (nuqta bilan, loyiha `-routing-module.ts` — chiziqcha bilan yozadi).
- "Ishlatilmayotgan fayl" — export qilingan klass/komponent hech qaysi
  boshqa faylda import qilinmagan (`grep -rl "ClassName" src/app` bo'sh
  natija bersa).

**Cheklovlar (Constraints):**
- Faqat `src/app/` ichida ishlang.
- O'chirishdan OLDIN har bir nomzod fayl uchun `grep -rn` bilan
  ishlatilmayotganini tasdiqlang — agar biror joyda import qilingan bo'lsa,
  UNI O'CHIRMANG, buning o'rniga `orcestor/analysis/T-018-duplicates.md`da
  "chalkash holat, qo'lda ko'rib chiqilsin" deb belgilang.
- `git status`/`git diff` orqali allaqachon boshqa task'lar tomonidan
  qasddan yaratilgan fayllarni (masalan yangi feature'lar) dublikat deb
  xato hisoblamang — faqat aniq bir xil maqsadga xizmat qiluvchi, boshqa
  fayl bilan mazmunan bir xil/o'xshash, hech qayerda ishlatilmaydigan
  fayllarni o'chiring.

**Ish doirasidagi fayllar (Files in scope):**
- O'qish va (tasdiqlangandan keyin) o'chirish uchun: `src/app/**`
- Yozish uchun: `orcestor/analysis/T-018-duplicates.md` (yangi)

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- `orcestor/analysis/T-018-duplicates.md`da: topilgan har bir shubhali fayl, uning holati (o'chirildi / chalkash-qo'lda ko'rilsin / soxta signal emas), sababi bilan jadval.
- Aniq dublikat deb tasdiqlangan fayllar o'chirilgan.
- `npm run build` xatosiz o'tadi (o'chirishdan keyin ham).

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven.

**Natijani qayerga yozish kerak (Report back):**
- `orcestor/task_pending/T-018-response.md`ga topilgan/o'chirilgan fayllar ro'yxatini yozing.
