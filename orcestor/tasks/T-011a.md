## Task ID: T-011a
### Sarlavha: Column-level jadval filter — 2-guruh (leaves, positions, terminals)

**Maqsad (Goal):**
Quyidagi 3 ta list sahifasiga `project_docs/design-reference.md` bo'lim
3.2 ("Column-level jadval filter patterni") ni qo'llash.

**Kontekst (Context):**
Ishlaydigan TO'LIQ namuna: `src/app/features/branches/pages/branch-list/
branch-list.ts` va `.html`. To'liq qoidalar: `project_docs/
design-reference.md` bo'lim 3.2.

**MUHIM CHEKLOV:** bu 3 ta sahifaning "Amallar" ustuni ALLAQACHON
to'g'ri (faqat delete tugmasi, SVG icon, `design-reference.md` bo'lim
3.3'dagi yakuniy kanonik pattern bo'yicha) — **actions-cell'ga,
`onRowClick` metodiga, delete metodiga HECH QANDAY o'zgartirish
kiritilmaydi**. FAQAT filter qatori qo'shiladi.

**Har bir sahifa uchun ustun-filter jadvali:**

### 1. `src/app/features/leaves/pages/leave-list/leave-list.ts` + `.html`
| Ustun | Filter turi | Manba |
|---|---|---|
| # | yo'q | — |
| Xodim | matn | `getEmployeeName(item.employeeId)` natijasi bo'yicha filtr (raw `employeeId` emas — metod natijasini pastki registrga o'tkazib solishtiring) |
| Turi | select | `item.type`: option qiymatlari — `vacation`(Hordiq ta'tili), `sick`(Kasallik ta'tili), `unpaid`(Ish haqisiz ta'til), `business_trip`(Xizmat safari), `other`(Boshqa), + "Hammasi" (bo'sh qiymat). Mavjud `getLeaveTypeLabel()` metodidan `<option>` label sifatida foydalanish shart emas — option matnini to'g'ridan-to'g'ri yozish mumkin. |
| Boshlanish sanasi | sana | `item.fromDate` |
| Tugash sanasi | sana | `item.toDate` |
| Kunlar | yo'q | — |
| Ish haqiga ta'sir | yo'q | — |
| Sabab / Izoh | yo'q | — |
| Amallar | yo'q (TEGMANG) | — |
Signal nomlari: `employeeFilter`, `typeFilter`, `fromDateFilter`,
`toDateFilter`. Metod/computed nomi: `filteredLeaves` (`leaves` bu
faylda plain array — `computed()` emas, oddiy metod yozing, mavjud
`leaves`/`employees` propertylariga to'g'ridan-to'g'ri murojaat qilib).
`@for`da `leaves` o'rniga `filteredLeaves()` ishlatiladi (agar hozir
`item of leaves` shaklida bo'lsa).

### 2. `src/app/features/positions/pages/position-list/position-list.ts` + `.html`
| Ustun | Filter turi | Manba |
|---|---|---|
| # | yo'q | — |
| ID | yo'q | — |
| Lavozim Nomi | matn | `position.name` |
| Kompaniya | matn | `getCompanyName(position.companyId)` natijasi bo'yicha |
| Filial | matn | `getBranchName(position.departmentId)` natijasi bo'yicha |
| Bo'lim | matn | `getDepartmentName(position.departmentId)` natijasi bo'yicha |
| Amallar | yo'q (TEGMANG) | — |
Signal nomlari: `nameFilter`, `companyFilter`, `branchFilter`,
`departmentFilter`. Metod nomi: `filteredPositions` (`positions` plain
array — oddiy metod).

### 3. `src/app/features/terminals/pages/terminal-list/terminal-list.ts` + `.html`
| Ustun | Filter turi | Manba |
|---|---|---|
| ID | yo'q | — |
| Nomi | matn | `terminal.name` |
| Filial ID | matn | `terminal.branchId` |
| IP manzil | matn | `terminal.ipAddress` |
| Amallar | yo'q (TEGMANG) | — |
Signal nomlari: `nameFilter`, `branchIdFilter`, `ipFilter`. Metod nomi:
`filteredTerminals` (`terminals` plain array — oddiy metod).

**Cheklovlar (Constraints):**
- FAQAT yuqoridagi 3 ta modulning `.ts` va `.html` fayllariga tegiladi
  (jami 6 fayl). CSS fayllarga tegilmaydi (filter uslubi global
  `styles.css`da allaqachon bor: `.column-filter-input`/
  `.column-filter-select`).
- Actions-cell/onRowClick/delete metodlariga ASLA tegilmaydi.
- `FormsModule` har bir `.ts`ga import qilinadi (agar yo'q bo'lsa).
- Yangi npm dependency qo'shilmaydi.

**Ish doirasidagi fayllar:** 6 ta fayl (3 modul × ts/html).

**Bajarilgan deb hisoblanish mezoni (DoD):**
- Filter qatori thead ichida, ishlaydi.
- "Filterga mos topilmadi" holati mavjud (asosiy bo'sh-holatdan alohida).
- Actions-cell o'zgarmagan (git diff'da ko'rinmasligi kerak).
- `npm run build` xatosiz o'tadi.

**Tavsiya etilgan rejim:** Agent-driven.

**Natijani qayerga yozish kerak:**
`orcestor/task_pending/T-011a-response.md`ga log, tugagach ikkala
faylni `orcestor/task_compliete/`ga ko'chiring.
