## Task ID: T-021
### Sarlavha: [Docs 2/3] API points+types hujjatlashtirish — Xodimlar/Davomat/Ta'til guruhi

**Ijrochi:** AGY

**Bog'liqlik:** Yo'q — T-020 bilan mustaqil, format bir xil bo'lishi uchun
faqat T-020'dagi namunaga rioya qiling.

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-senior-developer.md` uslubida.

**Maqsad (Goal):**
`employees`, `attendance`, `work-schedules`, `terminals`, `leaves`,
`holidays`, `advances` qismlarining barcha API "point"larini,
so'rov/javob tiplarini VA har bir sahifaning ichki holati (state)
qanday tuzilganini `project_docs/api/` ichiga har bir modul uchun
alohida `.md` fayl qilib hujjatlashtirish.

**Kontekst (Context):**
- T-020'dagi bilan bir xil: barcha so'rovlar `core/services/http.ts`
  (`Http` klassi) orqali o'tadi, har bir feature'ning `services/*.ts`
  fayli o'z endpoint'larini chaqiradi.
- `employees/components/face-register` — `Employee` servisi orqali
  (masalan `POST /employees/:id/face`) alohida endpoint chaqirishi mumkin,
  buni ham hujjatlang.
- `attendance/pages/scanner` — check-in/check-out uchun `Attendance`
  servisini chaqiradi, buni ham hujjatlang.
- Tiplar `src/app/core/models/*.ts` da.
- Holat (state) boshqaruvi loyihada izchil emas — ba'zi sahifalar oddiy
  class property, ba'zilari Angular `signal()` ishlatadi (T-020'da
  boshlangan kuzatuv — shu naqshni davom ettiring). Har bir sahifada
  qaysi yondashuv ishlatilganini aniq ro'yxatga oling.

**Cheklovlar (Constraints):**
- Hech qanday `.ts`/`.html`/`.css` kod fayliga tegilmaydi — faqat o'qish
  va `project_docs/` ichiga yozish.
- Format T-020'dagi bilan bir xil bo'lsin (pastga qarang).

**Ish doirasidagi fayllar (Files in scope):**
- O'qish uchun: `src/app/features/employees/**`,
  `src/app/features/attendance/**`, `src/app/features/work-schedules/**`,
  `src/app/features/terminals/**`, `src/app/features/leaves/**`,
  `src/app/features/holidays/**`, `src/app/features/advances/**`,
  `src/app/core/models/*.ts`
- Yozish uchun (yangi fayllar): `project_docs/api/employees.md`,
  `project_docs/api/attendance.md`, `project_docs/api/work-schedules.md`,
  `project_docs/api/terminals.md`, `project_docs/api/leaves.md`,
  `project_docs/api/holidays.md`, `project_docs/api/advances.md`

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- Har bir yuqoridagi modul uchun alohida `.md` fayl yaratilgan, quyidagi formatda:

  ```
  # <Modul nomi> — API

  ## Endpoint'lar
  | Metod | Yo'l | So'rov tipi | Javob tipi | Izoh |
  |---|---|---|---|---|
  | GET | /employees | — | Employee[] | Ro'yxat |
  | POST | /employees/:id/face | FormData/base64 | void | Yuz ro'yxatga olish |

  ## Tiplar
  ### Employee (core/models/employee.ts)
  (tipning to'liq interfeys ta'rifi shu yerga ko'chiriladi)

  ## State (sahifalar bo'yicha)
  | Sahifa | O'zgaruvchi | Turi (signal/property) | Boshlang'ich qiymat | Qanday yangilanadi |
  |---|---|---|---|---|
  | employee-list.ts | employees | property (Employee[]) | [] | loadEmployees() ichida to'g'ridan-to'g'ri tayinlash |
  ```
- Har bir feature service faylidagi HAR BIR chaqiruv hujjatda aks etgan.
- Har bir page/component faylidagi asosiy state o'zgaruvchilari "State" jadvalida aks etgan.
- Fayllar `project_docs/api/` papkasida, nomlash `<feature-nomi>.md` shaklida.

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven — faqat o'qish/yozish, past risk.

**Natijani qayerga yozish kerak (Report back):**
- Ishlash davomida **har bir muhim qadamni** darhol
  `orcestor/task_pending/T-021-response.md` fayliga yozib boring.
- Topshiriq to'liq bajarilgach:
  1. `T-021-response.md` oxiriga yakuniy xulosa qo'shing.
  2. Xulosadan keyin **"O'zgargan fayllar" jadvali**ni qo'shing (fayl | turi | qisqa sabab).
  3. `orcestor/tasks/T-021.md` va `orcestor/task_pending/T-021-response.md` ikkalasini `orcestor/task_compliete/` papkasiga ko'chiring.
