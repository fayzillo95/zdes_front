## Task ID: T-025
### Sarlavha: Axios'ni amalga oshirish

**Ijrochi:** AGY

**Bog'liqlik:** **T-024 tugagandan KEYIN** boshlanadi — bu task
`orcestor/analysis/T-024-axios-analysis.md`dagi tavsiya va rejaga qat'iy
rioya qiladi. T-024 nima tavsiya qilsa (kam-ta'sirli variant: faqat
`Http` ichida axios, `Observable<T>` tashqi interfeys saqlanadi / yoki
katta-ta'sirli variant), shuni bajaring — o'zingizcha boshqa yechim
tanlamang.

**Persona/uslub (agents/skills/):**
`agents/skills/engineering/engineering-frontend-developer.md` uslubida.

**Maqsad (Goal):**
`orcestor/analysis/T-024-axios-analysis.md`da tavsiya qilingan rejaga
asosan `axios`ni loyihaga qo'llash: `axios` dependency qo'shish,
`core/services/http.ts`ni axios asosida qayta yozish, mavjud
`auth-interceptor.ts`/`error-interceptor.ts` mantig'ini axios
interceptor'lariga ko'chirish.

**Kontekst (Context):**
- Bu vazifa T-024'ning tahlil hujjatidagi aniq rejaga bog'liq — task
  matnini o'zi umumiy, chunki aniq qadamlar T-024 natijasiga qarab
  belgilanadi.
- Agar T-024 "kam-ta'sirli" variantni tavsiya qilgan bo'lsa: faqat
  `core/services/http.ts` ichi o'zgaradi, tashqi `Observable<T>` interfeys
  saqlanadi (axios Promise'ini `from()` yordamida Observable'ga o'raladi),
  15+ feature service fayli **umuman o'zgarmaydi**.
- Agar T-024 "katta-ta'sirli" (butun loyiha Promise/async-await'ga
  o'tishi) variantni tavsiya qilgan bo'lsa — bu holatda ish hajmi katta
  bo'lgani uchun **T-025'ni ham T-017/T-019 uslubida bir necha kichik
  ketma-ket dispatchga bo'lib bajaring**, bitta so'rovda emas.

**Cheklovlar (Constraints):**
- T-024'dagi rejaga qat'iy rioya qiling — yangi arxitektura qarori
  ixtiro qilmang.
- `auth-interceptor.ts`/`error-interceptor.ts`dagi MAVJUD xatti-harakat
  (token biriktirish, 401'da logout) funksional jihatdan bir xil
  qolishi kerak — faqat mexanizm (Angular interceptor → axios
  interceptor) o'zgaradi.
- Har bir kichik dispatchdan keyin `npm run build` va `npx tsc --noEmit`
  bilan tekshiring.

**Ish doirasidagi fayllar (Files in scope):**
- `package.json` (axios qo'shish)
- `src/app/core/services/http.ts`
- `src/app/core/interceptors/auth-interceptor.ts`,
  `src/app/core/interceptors/error-interceptor.ts`
- `src/app/app.config.ts` (agar interceptor ulash usuli o'zgarsa)
- T-024 "katta-ta'sirli" variantni tavsiya qilgan bo'lsa: qo'shimcha
  ravishda `src/app/features/*/services/*.ts` va ularni chaqiruvchi
  `pages/**/*.ts` fayllar (T-024 hujjatidagi ustuvorlik ro'yxati bo'yicha)

**Bajarilgan deb hisoblanish mezoni (Definition of Done):**
- `axios` `package.json`ga qo'shilgan.
- `core/services/http.ts` T-024 rejasiga mos qayta yozilgan.
- Auth token biriktirish va 401'da logout xatti-harakati avvalgidek ishlaydi (funksional ekvivalent).
- Agar kam-ta'sirli variant tanlangan bo'lsa: hech qaysi feature service fayli o'zgarmagan (`git diff --stat` bilan tekshiring).
- `npm run build` va `npx tsc --noEmit` xatosiz o'tadi.

**Tavsiya etilgan Antigravity rejimi (Mode):**
Agent-driven, T-024 tavsiyasiga qarab bitta yoki bir necha kichik dispatchda.

**Natijani qayerga yozish kerak (Report back):**
- Har bir kichik dispatchdan keyin `orcestor/task_pending/T-025-response.md`ga qaysi fayllar o'zgartirilgani va T-024 rejasining qaysi qadami bajarilgani yozilsin.
- Topshiriq to'liq bajarilgach:
  1. `T-025-response.md` oxiriga yakuniy xulosa qo'shing.
  2. Xulosadan keyin **"O'zgargan fayllar" jadvali**ni qo'shing (fayl | turi | qisqa sabab).
  3. `orcestor/tasks/T-025.md` va `orcestor/task_pending/T-025-response.md` ikkalasini `orcestor/task_compliete/` papkasiga ko'chiring.
